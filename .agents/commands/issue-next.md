# Issue next

Use the `issue-execution` skill.

Inspect the open issues in `edemekong/good-ai`, their labels, dependencies, and current repository state. Select the highest-priority issue whose prerequisites are satisfied. Prefer dependency order over issue number. Report the selected issue, why it is next, its scope/non-goals, and any clarification that is genuinely required. If no clarification is required, begin implementation in the same turn.

Do not start a second implementation issue until the active issue is validated and handed off.
