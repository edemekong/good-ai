# Issue handoff

Use the `issue-execution` skill.

Prepare a delegation-ready handoff for one GitHub issue. Include the issue URL, goal, exact scope, non-goals, PRD sections, relevant files, dependencies, assumptions, expected deliverables, validation commands, and stop conditions. Do not assign or delegate externally unless the user explicitly asks for that action.
